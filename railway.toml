[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/api/sites"
healthcheckTimeout = 30

[build.nixpacksPlan.phases.setup]
nixPkgs = ["python311"]

[source]
rootDirectory = "ess-backend"
